image: leanprovercommunity/mathlib3:gitpod

vscode:
  extensions:
    - jroesch.lean

tasks:
  - init: . /home/gitpod/.profile && leanpkg configure && leanproject get-mathlib3-cache && leanproject build
